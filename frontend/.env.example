# Copy to .env.local and fill in your values

# Backend chat model (Anthropic Claude). Get key at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Embeddings (RAG + ingest notebook) — OpenAI
OPENAI_API_KEY=sk-...

# Optional: Vercel AI Gateway key (replaces per-provider keys)
# Get one free at https://vercel.com/~/ai/api-keys ($5/month free credit)
# AI_GATEWAY_API_KEY=...
